<footer class="my-20">
  <div class="mx-auto max-w-xl px-4">
    <h2 class="text-center text-lg font-semibold text-slate-800">Get notified at stable launch</h2>
    <form name="product-launch" method="POST" data-netlify="true" netlify-honeypot="bot-field" class="mt-4 flex flex-col sm:flex-row gap-3">
      <input type="hidden" name="form-name" value="product-launch" />
      <p class="hidden">
        <label>Don&apos;t fill this out if you're human: <input name="bot-field" /></label>
      </p>
      <label for="email" class="sr-only">Email address</label>
      <input
        id="email"
        name="email"
        type="email"
        required
        placeholder="you@example.com"
        class="w-full rounded-md border border-slate-300 bg-white px-4 py-2 text-slate-900 placeholder-slate-400 focus:border-slate-400 focus:outline-none focus:ring-2 focus:ring-slate-200"
      />
      <button type="submit" class="shrink-0 rounded-md bg-slate-900 px-5 py-2 text-white hover:bg-slate-800 focus:outline-none focus:ring-2 focus:ring-slate-300">Notify me</button>
    </form>
    <p class="mt-3 text-center text-xs text-slate-500">We’ll email you when the product is stable and launched.</p>
  </div>
  <p class="mt-10 text-center text-sm text-slate-500">
    Copyright © {new Date().getFullYear()} Windbase. All rights reserved.
  </p>
</footer>
